Scriptname mzinInterfaceOCum

; BUILD-ONLY STUB for SLSurvival compilation.
; The real implementation needs the OCum mod's OCumScript type, which is not installed here.
; The installed Bathing in Skyrim - Renewed .pex provides the real behavior at runtime;
; SLSurvival never calls this. Signature preserved for the compile closure.

Function OCClearCum(Quest kOCumScript, Actor akTarget) Global
EndFunction
