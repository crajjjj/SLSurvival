Scriptname zadInterfaceVRIK Hidden

; BUILD-ONLY STUB for SLSurvival compilation.
; The real implementation needs VRIK source (_vrikAction_qust_mcm / vrikActions), which
; is not installed here. The installed Devious Devices NG .pex provides the real VR
; behavior at runtime; SLSurvival never calls these. Signatures match zadLibs' call sites.

Function EnableVR(Quest _API) Global
EndFunction

Function DisableVR(Quest _API) Global
EndFunction
